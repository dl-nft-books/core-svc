log:
  disable_sentry: true

db:
  url: "postgres://book:book@localhost:25432/book?sslmode=disable"

eth_minter:
  eth_signer:
  precision: 18

listener:
  addr: :8000

cop:
  disabled: true
  endpoint: "http://..."
  upstream: "http://..."
  service_name: generator-svc
  service_port: 80
